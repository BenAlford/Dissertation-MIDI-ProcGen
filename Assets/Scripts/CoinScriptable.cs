using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "CoinCounter", menuName = "CoinCounter")]
public class CoinScriptable : ScriptableObject
{
    public int coins = 0;
}
